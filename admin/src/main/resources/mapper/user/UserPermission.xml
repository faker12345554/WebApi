<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.admin.admin.dao.user.UserPermissionGroupDao">
    <select id="listGroup" resultType="com.admin.admin.entity.user.UserPermissionGroup">
        SELECT * from t_userpermissiongroup WHERE status=#{flag}
    </select>

    <select id="getGroup" parameterType="int" resultType="com.admin.admin.entity.user.UserPermissionGroup">
        SELECT * from t_userpermissiongroup WHERE status=TRUE and permissionid=#{id}
    </select>
    <insert id="saveUserGroup" parameterType="com.admin.admin.entity.user.UserPermissionGroup">
        insert into t_userpermissiongroup(createid,createtime,permissionname,createname,status) values (#{createid},#{createtime},#{permissionname},#{createname},#{status})
    </insert>
    <update id="updateGroup" parameterType="com.admin.admin.entity.user.UserPermissionGroup">
        update t_userpermissiongroup set permissionname=#{permissionname},createtime=#{createtime},createid=#{createid},createname=#{createname},status=#{status} where permissionid=#{permissionid}
    </update>
    <update id="deleteGroup" parameterType="com.admin.model.ParamterModel">
        update t_userpermissiongroup set status=#{flag} where permissionid=#{id}
    </update>
    <select id="selectByName" parameterType="String" resultType="int">
        select count(*) from t_userpermissiongroup where permissionname=#{Name}
    </select>
</mapper>