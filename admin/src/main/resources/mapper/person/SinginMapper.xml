<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.admin.admin.dao.person.SinginDao">
    <select id="getSingin" parameterType="int" resultType="com.admin.admin.entity.person.SinginInformation">
        select * from t_singin where person_id=#{person_id}
    </select>

</mapper>