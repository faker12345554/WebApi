<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.adminapp.business.dao.dw_login.LoginDao">
    <select id="Login" parameterType="String" resultType="com.adminapp.business.entity.dw_login.UserInformation">
        select * FROM t_user WHERE accountname=#{accountname}
    </select>
</mapper>